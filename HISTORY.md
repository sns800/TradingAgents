# G20 매크로·정치 대시보드 프로젝트 이력 (HISTORY)

> **이 문서의 범위**: 2026-09-20에 진행한 **G20 매크로·정치 대시보드** 프로젝트 하나의 전체 경과만
> 담습니다. 저장소 전체(포크 이관·한글화·웹 UI·프롬프트 교정·카탈로그 등 30항)의 이력은
> [작업이력.md](작업이력.md)에 있고, 이 프로젝트는 그중 **30항**에 한 줄로 요약돼 있습니다.
>
> **기록 규약**: 가계부 프로젝트의 HISTORY 규약과 같은 **요청 → 변경 → 이유** 3단으로 각 단계를
> 적습니다. 시각은 모두 **KST(2026-09-20)** 이며, 표의 수치는 실행 로그·`git show --stat`·
> 테스트 출력·저장소 파일에서 확인한 값만 씁니다.
>
> 구조·그림 중심 종합 문서는 [webui/macro/구조와-작업내역.md](webui/macro/구조와-작업내역.md),
> "어떻게 구현했는가"는 [webui/macro/구현-상세.md](webui/macro/구현-상세.md),
> 키·스키마·API의 단일 기준은 [webui/macro/CONTRACT.md](webui/macro/CONTRACT.md)입니다.

---

## 1. 문서 목적·범위

| 구분 | 내용 |
|---|---|
| 목적 | 이 프로젝트가 **어떤 요청에서 시작해 무엇을 만들고 무엇을 남겼는지**를 시간순으로 추적 가능하게 남긴다 |
| 대상 | 제안 → 사용자 결정 → 데이터 계약 → 멀티에이전트 구현 3차 → 검증 → 배포·커밋·PR → 부트스트랩 수집 → API 키 적용 → Ember 유로존 보완(완료) |
| 비대상 | 저장소의 다른 작업(분석 프레임워크·프롬프트·카탈로그 등) → [작업이력.md](작업이력.md) / 웹 UI 전반 → [webui/변경이력.md](webui/변경이력.md) |
| 갱신 규칙 | 새 작업이 끝날 때마다 2장 타임라인에 행 1개, 3장에 "요청 → 변경 → 이유" 항목 1개를 추가한다. 예정 작업은 5장 표에서 관리한다 |

---

## 2. 타임라인 (2026-09-20 · KST)

| 시각 | 단계 | 산출물 | 검증 |
|---|---|---|---|
| 오전 ~13:10 | 요청 접수 → 기존 프로젝트 탐색(`fx-split`이 아니라 `TradingAgents/webui`임을 확인) → 데이터 소스 조사(BIS·OECD·IMF·Ember 웹 검색) | [webui/G20-매크로-대시보드-제안.md](webui/G20-매크로-대시보드-제안.md)(9장), 동작 목업 [webui/mockups/g20-macro-mockup.html](webui/mockups/g20-macro-mockup.html) + 스크린샷 6장 | 목업 색 팔레트 검증기 통과(다크 표면 #161b22 기준) |
| ~13:20 | 사용자 결정 5가지 확정 | 결정 사항이 제안서 9장 → CONTRACT 0장으로 고정 | — |
| 13:20~ | 데이터 계약 고정 후 **1차 웨이브 7개 에이전트 병렬** (레지스트리·집계 Opus / 저장소 Opus / 정량 소스 A Opus / 정량 소스 B Opus / LLM 계층 Opus / 프론트 **Fable** / 인프라·문서 Opus) | `CONTRACT.md`·`schema.py`, `registry.py`+`registry.yaml`, `aggregate.py`, `store.py`, `sources/*` 10개, `llm/*` 4개, `frontend/macro.js`·`charts.js`, `infra/template.yaml` | 각 소스가 **실제 API를 호출해** 키·차원 순서·커버리지 확정(아래 3.5) |
| 1차 완료 후 | **2차 웨이브 3개 병렬**: 계약·레지스트리 정합(Opus) / Lambda API(Opus) / 수집기 진입점 + 로컬 검증 서버(**Fable**) | 정합 17건 반영, `webui/backend/macro_api.py`, `webui/macro/collect.py`·`devserver.py` | API 39테스트 통과, 6개국 dry-run **관측치 5.5만 건** → 로컬 검증 서버에서 실제 프론트 스크린샷 검수 |
| 2차 완료 후 | **3차**: 통합 후속 6건(Opus) / 읽기 전용 코드 리뷰(Opus) / 리뷰 지적 18건 수정(**Fable**) | US 환율 기준값 합성·LLM 원문 저장·해시 노출·`FakeTable` 60배 가속·히트맵 지연 배지·imf 로그, 리뷰 지적 18건 수정 | 리뷰 결과 높음 3 · 중간 8 · 낮음 12 → 18건 수정 + 회귀 테스트 추가 |
| ~16:50 | 코디네이터 최종 검증 | — | 전체 **pytest 1,467 passed**(매크로 409), `ruff`·`node --check`·`validate-template` 통과, 수집기 재실행 + 검증 서버 **8개 GET 200** |
| 16:50~17:10 | 문서화 | `webui/macro/구조와-작업내역.md`(872줄·mermaid 11개), [작업이력.md](작업이력.md) 30항, [webui/변경이력.md](webui/변경이력.md), `webui/macro/README.md`, [webui/아키텍처.md](webui/아키텍처.md) 5장 | 문서 내 링크·수치를 코드·덤프에서 재추출 |
| 17:04~17:09 | `webui/infra/deploy.sh` 실행 | 스택 `UPDATE_COMPLETE`, CodeBuild 워커 이미지, Lambda, 프론트 배포 | 배포 완료(17:09) |
| 17:10 | 단일 커밋 → 푸시 → PR 생성 | 커밋 `a0f7fc4` (**105파일 +29,642 / −107**, 8월 카탈로그 개편 포함), **PR #1** <https://github.com/sns800/TradingAgents/pull/1> | `git show --stat a0f7fc4` |
| 17:11~17:36 | 부트스트랩 수집 태스크(`--since 2015-01-01 --force`) | 관측치·문서·집계·LATEST/SNAPSHOT 일괄 생성 (상세 4장) | **25.4분 · exit 0**, 결과를 PR 코멘트로 기록 |
| 18:50~18:57 | 사용자가 FRED·Ember API 키 발급 → `.env` 저장 → 재배포 → 검증 수집 | 태스크 정의 **리비전 3**에 키 반영, `fred` 3,612건 · `ember` API 2,178건 | 키 형식 검증 + 라이브 인증(FRED 200, Ember는 `api_key` **쿼리만** 허용), `deploy.sh --skip-image` 성공, 검증 태스크 **97초** |
| 19:10~19:41 | Ember 보완: 유로존은 API 집계 엔티티 `entity=EU`로 수집(요청 분리), API 미제공 국가만 공개 CSV 보충, 인증 쿼리 우선 | 커밋 fc44ad1(5파일 +405/−50) → 이미지 재배포(19:26~19:39) → 검증 태스크 54초 | 테스트 413 passed · 운영: `LATEST#elec_mix` EU **M 2026-08 · flags 없음**, 20/20개국, 갱신 139 · 오류 0 · CSV 다운로드 0회 |

---

## 3. 단계별 "요청 → 변경 → 이유"

### 3.1 제안 — 요구사항을 수집 가능한 지표로 번역 (오전 ~13:10)

**요청**
> "다음에 관련된 각각의 데이터들을 저장하고, 월/분기/년 단위로 데이터는 시계열로 시각화하고,
> 개별 데이터들도 세부내용을 확인해보고 싶어. 기존의 stock.happymstn.com 프로젝트에 연결해서
> 프로젝트를 진행하고 싶어. 정치/경제/AI에 20년 이상 숙련된 전문가로서 이를 달성하기 위한
> 방안과 화면 디자인을 제안해줘." (정당 지지율 / 금리·중앙은행 기조·통화량·환율 / 경제구조·
> 에너지 / GDP·GNI·예산·국방비 / PPI·CPI·집값)

**변경**
- 기존 프로젝트 위치를 먼저 확정했다 — `fx-split`이 아니라 이 저장소의 `webui/`(stock.happymstn.com).
- 5개 요구 영역을 **수집 가능한 11개 원천 × 38개 지표**로 번역하고, 원천별 접근 가능성(무키/유키, 커버리지)을
  웹 검색으로 사전 조사(BIS·OECD·IMF·Ember)했다.
- 제안서 `webui/G20-매크로-대시보드-제안.md`(9장: 현황 진단 → 지표 매핑 → 소스 전략 → 아키텍처 →
  화면 설계 → 로드맵 → 비용 → 리스크 → 결정 사항)와 **브라우저에서 동작하는 목업**
  `webui/mockups/g20-macro-mockup.html`(5개 화면 + 관측치 드로어)을 만들고 스크린샷 6장을 남겼다.

**이유** 화면 설계를 글로 합의하면 구현 단계에서 재작업이 생긴다. 동작 목업으로 색·밀도·차트 종류를
먼저 확정하고, 그 목업 코드를 그대로 `charts.js`의 출발점으로 썼다. 지표를 먼저 원천에 매핑한 것은
"수집 불가능한 지표를 화면에 그리는" 설계를 피하기 위함이다.

### 3.2 사용자 결정 5가지 (~13:20)

**요청**
> "17개국 전부 대상으로, 저장소는 DynamoDB, 배치는 기존 스택과 합쳐서, 모두 추천대로 적용해줘.
> 수행은 멀티에이전트 방식으로 최적의 모델을 선택해서 실행해줘."
> (추가 확인 2건: "배포는 기존 사이트에 통합되는 형태야?" → 예. "커밋도 기존 리포에 추가되는 형태야?" → 예)

**변경** 결정 5가지를 CONTRACT 0장에 고정했다 — ① 여론조사 **17개국 전부**(중국·사우디·유로존은
`not_applicable` 문서로 사유 명시) ② **DynamoDB 단일 테이블** `tradingagents-webui-macro` + S3 원본 보존
(Parquet/Athena는 후속) ③ **기존 CloudFormation 스택에 리소스만 추가**, 워커 이미지 공용(`command`만 교체)
④ **AI 판정은 검토 전에도 노출** + `ai_generated` 배지·신뢰도·인용·원문 링크·`review_status`
⑤ **금액은 USD 통일**, 한국만 원화 병기.

**이유** 결정을 계약 문서 0장에 박아두면 7~10개 에이전트가 병렬로 움직여도 같은 전제로 구현한다.
"빈칸"과 "해당 없음"을 구분한 것(①), 새 스택·새 이미지를 만들지 않은 것(③)은 각각 데이터 신뢰성과
운영 대상 수를 늘리지 않기 위한 선택이다.

### 3.3 데이터 계약 고정 (13:20)

**요청** "모두 추천대로 적용" + 멀티에이전트 병렬 실행 지시.

**변경** 구현 착수 **전에** `webui/macro/CONTRACT.md`(264줄)와 `webui/macro/schema.py`를 확정했다 —
국가 20개, 지표 38개(id 고정 목록·순서까지), Observation/Doc 스키마, DynamoDB 키 7종, 집계 규칙,
소스 모듈 인터페이스, S3 레이아웃, API 10개 경로(+9.1절 프론트 의존 규칙), 환경변수, 프론트 계약,
케이던스·폴백 체인.

**이유** 병렬 구현에서 가장 비싼 실패는 "층마다 키·필드가 다른 것"이다. 계약을 먼저 얼려서
수집기·저장소·API·프론트 4개 층이 서로를 기다리지 않고 동시에 작업할 수 있게 했다.

### 3.4 1차 웨이브 — 7개 에이전트 병렬 (13:20~)

**요청** "에이전트를 나누어 독립된 작업들은 최대한 병렬로 빠르게 실행되게 해주고, 작업에 적합한
모델을 선택해서 최적의 실행을 진행해줘."

**변경** 모델을 작업 성격에 맞춰 배정했다(정밀 구현·리뷰 = Opus, 화면·대량 코드 = **Fable**).
① 레지스트리·집계(`registry.py`·`registry.yaml`·`aggregate.py`) ② 저장소(`store.py`)
③ 정량 소스 A(BIS·OECD·World Bank·IMF) ④ 정량 소스 B(FRED·Ember·OWID·WITS·Yahoo·companies)
⑤ LLM 계층(`llm/*` 4개 소스 + `bedrock_json`·`textutil`) ⑥ 프론트(`macro.js` 5뷰 + `charts.js`)
⑦ 인프라·문서(`template.yaml`·`deploy.sh`·Dockerfile).

각 소스 담당은 **실제 API를 호출해** 제안서 부록의 가정을 정정했다(모두 모듈 상단 주석에 기록):
- BIS `WS_XRU` 키는 `M.{국가}.USD.A`가 아니라 **`M.{국가}.{해당국 통화}.E`**(기말) — 3번째 차원이 대상국 통화.
- OECD에는 PPI 전용 데이터플로가 없고 KEI `MEASURE=PP`는 원천이 **2023-02에서 정지**.
- IMF MFS `BM_MAI.XDC.M`은 20개국 중 **11개국만** 제공(US·CN·EU·GB·IN·SA 미제공).
- Ember는 **키 없이 받을 수 있는 공개 CSV**가 있고 유로존이 `Area="EU"`로 들어 있음.
- WITS `tradestats-trade`의 product 차원은 HS 챕터 97개가 아니라 **품목군 16개**.
- yfinance 시총은 런던 `GBp`·요하네스버그 `ZAc`가 **보조통화**라 ÷100 필요.
- 위키 여론조사 문서는 17개국 중 **15개국**만 규칙 파싱 가능(IN은 의석 예측표, TR은 영문 문서 없음).

**이유** 공개 통계 API는 문서와 실제가 자주 다르다. 실측으로 키를 고치지 않으면 배포 후 "값이 비는"
장애가 소스별로 흩어져 발생한다. 커버리지 공백은 숨기지 않고 `registry.yaml`의 `coverage_note`와
모듈 주석에 남겨 후속 소스 선정의 근거로 쓰게 했다.

### 3.5 2차 웨이브 — 계약 정합 · API · 수집기

**요청** 동일(병렬·최적 모델).

**변경** ① 계약·레지스트리 정합 검증에서 **17건**을 반영(단위·빈도·소스 우선순위 불일치 등)
② Lambda API `webui/backend/macro_api.py` — `/api/macro/*` 8개 GET + 관리자 2개 POST, boto3만 사용,
테스트 39건 ③ 수집기 진입점 `webui/macro/collect.py` + 로컬 검증 서버 `webui/macro/devserver.py`.
6개국(KR·US·JP·EU·DE·BR) dry-run으로 **관측치 5.5만 건**을 모아 덤프하고, 그 덤프를 devserver에 올려
실제 `api_handler.py`·`macro.js`로 5개 화면을 렌더링해 스크린샷(`webui/mockups/screenshots/live-*.png` 8장)을 남겼다.

**이유** 목업이 아니라 **실데이터 + 실제 프론트 코드**로 화면을 확인해야 "빈 셀·지연 데이터·폴백 표기"처럼
합성 데이터에서는 드러나지 않는 문제가 보인다. devserver는 Cognito·AWS 없이 Lambda 핸들러를 그대로
구동하므로, 배포 전에 API 계약 위반을 잡을 수 있다.

### 3.6 3차 웨이브 — 통합 후속 · 코드 리뷰 · 리뷰 수정

**요청** 동일.

**변경**
- **통합 후속 6건**: US 환율 기준값(1.0) 합성, LLM 원문 S3 보존, 에너지 정책 원문 해시 노출,
  `FakeTable` 파티션 인덱스로 **60배 가속**(55k 항목 질의 221초 → 4초 미만), 히트맵 지연 배지, imf 경고 로그.
- **읽기 전용 코드 리뷰**(별도 에이전트): 높음 3 · 중간 8 · 낮음 12 지적.
- **리뷰 지적 18건 수정**: 케이던스 날짜 경계 판정, 실패·부분 실패 소스 **1일 뒤 재시도**,
  여론조사 W→M 집계 가드, API 기간 상한(30년 · D 10년 · 국가당 4,000점), 수집 락 + `refresh` 409,
  링크 스킴 화이트리스트, 후처리 단계별 예외 격리, 미사용 IAM 액션 제거, `NoEcho` 한계 문서화,
  `lcu_bn` 단위 지표 LATEST rank 제외, BIS 기간 형식 검증, LLM 예산 CONFIG 우선, CSV 수식 접두 방어,
  툴팁 순위 표기, 관리자 승인/반려 UI, `MacroTable` 재생성 절차.

**이유** 구현 에이전트와 리뷰 에이전트를 분리해 "자기 코드를 자기가 검토하는" 편향을 줄였다.
지적 중 다수(케이던스·재시도·기간 상한·락)는 **운영 중에만 드러나는 결함**이라 배포 전에 닫아야 했다.

### 3.7 최종 검증 (~16:50)

**요청** "커밋, 푸시 PR생성과 배포 모두 진행해줘" (선행 조건으로 전체 검증).

**변경** 전체 `pytest` **1,467 passed**(매크로 신규 409건), `ruff check webui/macro webui/backend`
통과, `node --check` 4/4, `aws cloudformation validate-template` 통과, 수집기 재실행 후 검증 서버
**8개 엔드포인트 GET 200 + `ok: true`**.

**이유** 기존 실패 1건(`test_bedrock_provider::test_construction_when_extra_installed`)은 셸
`AWS_REGION` 환경 의존이라 이번 작업과 무관함을 확인하고 기록에 남겼다 — 배포 판단에서 "무관한 빨간불"이
섞이지 않게 하기 위함이다.

### 3.8 배포 · 단일 커밋 · PR (17:04~17:10)

**요청** "커밋은 한번에 진행해줘. 모든 작업내역과 상세설명 및 구조(시각화 포함)내역 추가하는 것도 포함해줘."

**변경** 문서 4종을 먼저 완성한 뒤 `webui/infra/deploy.sh`(17:04~17:09, 스택 `UPDATE_COMPLETE` →
CodeBuild 워커 이미지 → Lambda → 프론트) → 단일 커밋 `a0f7fc4`(**105파일 +29,642/−107**) → 푸시 →
**PR #1**(<https://github.com/sns800/TradingAgents/pull/1>).

**이유** 사용자 지시대로 커밋을 한 번으로 묶었고, 같은 워킹트리에 있던 8월 카탈로그 개편
(작업이력 28·29항)의 미커밋 변경도 함께 포함했다 — 분리하면 카탈로그 변경만 남은 워킹트리가
테스트를 깨뜨릴 수 있어 사용자 결정으로 한 커밋에 넣었다.

### 3.9 부트스트랩 수집 (17:11~17:36)

**요청** 첫 실행은 과거 시계열까지 채울 것(제안서 로드맵: 통화가치 지수는 12개월 전 기준값이 필요).

**변경** `--since 2015-01-01 --force` 태스크 1회 실행 — **25.4분 · exit 0**. 결과는 PR 코멘트에 기록.

| 구분 | 결과 |
|---|---|
| 정량 소스 | bis **62,975** · oecd 8,360 · worldbank 4,717 · yahoo 4,650 · ember(CSV) 2,317 · imf 1,302 · owid 570 · wits 18 · companies 18 (합계 **84,927건**) |
| 정성 소스 | cb_statements 7문서(partial) · wiki_polls 549문서/529관측(partial) · energy_policy 9문서(partial) · weekly_brief **실패**(Bedrock 연결 끊김 1회) |
| 후처리 | 파생 4,218 · 저빈도 집계 8,504 · poll_of_polls 13개국 · LATEST 623 · SNAPSHOT 20 |
| LLM | 토큰 **91,942**(일일 예산 2,000,000의 약 4.6%) |

**이유** 30년 치를 매일 다시 받으면 API 제한에 걸리므로, 부트스트랩 1회 뒤에는 `INGEST#<source>`의
마지막 실행일 − 45일만 증분 수집한다. `weekly_brief` 실패는 소스 단위 격리 덕에 나머지 결과에 영향을
주지 않았고, 실패·부분 실패는 `next_due`가 다음 날로 앞당겨져 자동 재시도된다.

### 3.10 API 키 발급·적용·검증 (18:50~18:57)

**요청** 사용자가 FRED·Ember 키를 직접 발급해 전달.

**변경** 키를 저장소 루트 `.env`에 저장(코디네이터가 형식 검증 + 라이브 인증 확인 —
FRED 200, **Ember는 헤더가 아니라 `api_key` 쿼리 파라미터만 허용**) → `deploy.sh --skip-image` 성공 →
ECS 태스크 정의 **리비전 3**에 키 반영 확인 → 검증 태스크(`--sources fred,ember --force`, **97초**):
`fred` **3,612건 ok**, `ember` API **2,178건 ok**(유로존 미수집), 미국 PPI 2026-08·M2 2026-07이 FRED 값으로
갱신, 한국 `elec_mix`의 대체 소스 플래그 제거.

**이유** 키가 필요한 소스는 없어도 경고 한 줄로 스킵되게 설계했으므로(부트스트랩에서 `fred` 0건),
키가 생긴 뒤 **그 두 소스만** 다시 돌려 최신성 공백을 메우면 된다. Ember 인증 방식은 키가 없던 구현
시점에 "헤더 우선 → 쿼리 재시도"로 만들어 두었는데, 실제로는 쿼리만 통했다.

### 3.11 Ember 유로존 보충 (19:10~19:41 · 완료)

**요청** 검증에서 드러난 공백(Ember API가 유로존을 주지 않음) 보완.

**변경** Ember 수집 경로를 **4단**으로 보완했다 — ① API → ② **API 응답에 없는 국가만**
공개 CSV 보충 → ③ 공개 CSV 전체 → ④ OWID. 유로존은 `entity_code`(3자 ISO) 옵션에 없고 집계 엔티티
`entity=EU`로만 오며 두 파라미터를 한 요청에 섞으면 0행이므로 요청을 나누고, 인증은 **쿼리 파라미터 우선**으로
바꿔 통한 방식을 기억하게 했다. 커밋 fc44ad1 → `deploy.sh`(워커 이미지 재빌드) → 운영 검증 태스크(`--sources ember --force`)
결과: 20/20개국, 유로존 `elec_mix` **M 2026-08이 API에서** 들어와 `fallback_source` 플래그가 제거됐고(갱신 139건), 공개 CSV
다운로드 0회, `[ember] elec_mix 미수집 1개국: EU` 로그 소멸. 인도네시아·사우디는 Ember에 월간 시계열이 없어 연간(2024)만 유지된다.

**이유** 유로존은 개요 히트맵·국가 프로필의 기본 국가라 `elec_mix`가 비면 눈에 띈다. API의 국가 코드
목록에는 유로존이 없어 **집계 엔티티 요청을 따로 보내야** 하고, 그 요청까지 실패하면 공개 CSV가 유로존을
덮는 유일한 경로다. 그래서 "API가 주는 국가는 API로, 빠진 국가만 CSV로" 채우는 경로가 필요하다.
추가 발견: 유효한 키로도 `X-API-Key` 헤더는 403이며 `api_key` 쿼리만 통한다(공식 문서와 일치).

---

## 4. 수행한 작업 및 결과 요약

| 영역 | 결과 | 근거 수치 |
|---|---|---|
| 제안·설계 | 요구 5영역 → 원천 11개 · 지표 38개로 번역, 동작 목업으로 화면 확정 | 제안서 473줄(9장) · 목업 스크린샷 6장 · 실데이터 스크린샷 8장(`webui/mockups/screenshots/`) |
| 데이터 계약 | 4개 층이 공유하는 단일 기준 확정 후 구현 착수 | `CONTRACT.md` 264줄 · 12장 구성 · `schema.py` dataclass 2종 |
| 레지스트리 | 국가·지표 사전을 YAML 1개로 통합, 로드 시 계약 위반 일괄 검증 | `registry.yaml` 국가 **20** · 지표 **38** · 검증 실패는 ValueError 1회에 모아 보고 |
| 수집기 | 정량 10 + 정성 4 소스, 실행 순서 고정 + 단계별 실패 격리 | `collect.py` 1,559줄 · 소스 모듈 14개 · 케이던스 4종(daily/weekly/monthly/quarterly) |
| 저장소 | DynamoDB 단일 테이블(키 7종) + S3 원본/문서 보존, 개정 이력 10건 | `store.py` · `MAX_REVISIONS=10` · 락 TTL 3시간 |
| API | `/api/macro/*` 8개 GET + 관리자 2개 POST, boto3만으로 Lambda zip 배포 | `macro_api.py` · 테스트 **43건** · 기간 상한 30년/D 10년/4,000점 |
| 프론트 | 5개 뷰 + 관측치 드로어, 외부 차트 라이브러리 없음 | `macro.js`·`charts.js` · 인라인 SVG 프리미티브 7종(line·hbars·divBars·stackedH·heatmap·meter·sparkline) |
| 인프라 | 기존 스택에 리소스만 추가(새 스택·새 이미지 없음) | DynamoDB 1개(PITR·Retain) + EventBridge 1개(`cron(0 21 * * ? *)`) + 파라미터 5개 |
| 테스트 | 매크로 신규 409건, 전체 스위트 유지 | 전체 **1,467 passed** · 매크로 409(8파일 6,138줄) · 픽스처 19개 |
| 코드 리뷰 | 구현·리뷰 에이전트 분리, 지적 전량 반영 | 지적 23건(높음 3·중간 8·낮음 12) 중 **18건 수정** + 회귀 테스트 |
| 문서 | 구조·계약·운영·이력 4종 + 아키텍처 5장 신설 | `구조와-작업내역.md` 872줄 · mermaid **11개** · `아키텍처.md` mermaid 4개 |
| 배포·커밋·PR | 기존 사이트에 탭 하나로 통합 배포, 단일 커밋 | `a0f7fc4` **105파일 +29,642/−107** · PR #1 · 스택 `UPDATE_COMPLETE` |
| 부트스트랩 | 2015년 이후 시계열 일괄 적재 | **25.4분 exit 0** · 정량 84,927건 · 파생 4,218 · 집계 8,504 · LATEST 623 · SNAPSHOT 20 · LLM 91,942토큰 |
| API 키 적용 | FRED·Ember 최신성 공백 해소 | 검증 태스크 **97초** · fred 3,612 · ember 2,178 · 태스크 정의 리비전 3 |

---

## 5. 미수행 작업 · 예상 작업 과정 · 필요 여부

> "필요 여부"는 **한국 사용자 기준 대시보드 가치 × 대체 수단 유무**로 판단했습니다.
> 20개 항목이며, 각 행의 마지막 열에 판단 근거를 한 줄씩 적었습니다.

| # | 항목 | 필요 여부 | 예상 과정 · 규모 | 판단 근거 |
|---|---|---|---|---|
| 1 | **한국은행 ECOS 소스** (한국 PPI·M2 최신성) | **높음** | ECOS 인증키 발급 → `webui/macro/sources/ecos.py`(통계표 코드 매핑: 생산자물가지수·통화량 M2) → `registry.yaml` 소스 우선순위 등록 → 테스트 → 워커 이미지 재배포. **반나절** | 한국이 이 대시보드의 주 사용자인데 OECD KEI에 KR PPI가 없고(원천 2023-02 정지), IMF MFS의 KR M2는 최신이 2025-10에서 멈춘다 |
| 2 | **Eurostat PPI** (유로존·독일·프랑스·이탈리아) | 중간 | Eurostat SDMX에서 산업생산자물가 데이터셋 확인(데이터플로 id는 탐색 필요) → `sources/eurostat.py` → registry 등록 → 테스트. **2~3시간** | SDMX가 표준적이고 무키라 난이도가 낮다. 유로존 3개국 PPI 열이 비어 있어 비교 화면에서 즉시 보인다 |
| 3 | **일본 e-Stat · 중국 NBS · 인도 PPI** | 중간~낮음 | 기관별 인증·응답 형식이 서로 달라 소스 모듈 3개를 따로 작성. 국가당 **반나절~1일** | 지표 공백은 같지만 API 난이도(인증·일본어/중국어 코드 체계·비표준 응답)가 높아 투입 대비 회수가 낮다 |
| 4 | **ECB 통화량 M3** (유로존 `m2_level` 없음) | 중간 | ECB Data Portal SDMX에서 통화량(M3) 시리즈 확인(데이터플로 id는 탐색 필요) → 기존 SDMX 헬퍼(`sdmx_csv_to_rows`) 재사용 → registry에 EU 전용 엔트리. **2~3시간** | IMF MFS에 유로존이 아예 없어 통화량 화면에서 EU가 빈다. ECB SDMX는 무키·안정적이라 구현이 쉽다 |
| 5 | **IMF Fiscal Monitor 정부부채 폴백** (9개국) | 중간 | IMF SDMX 3.0 데이터플로 전수 탐색으로 일반정부 총부채 시리즈 확정 → `sources/imf.py`에 지표 추가 → 테스트. **반나절**(탐색이 대부분) | World Bank `GC.DOD.TOTL.GD.ZS`에 AR·CN·DE·EU·FR·ID·IT·JP·SA가 없어 재정 카드의 9개국이 빈다. 다만 연간 지표라 급하지 않다 |
| 6 | **아르헨티나 정책금리** (BCRA API) | 낮음 | BCRA 공개 API의 정책금리 시리즈 확인(엔드포인트는 탐색 필요) → `sources/bcra.py` → AR 전용 엔트리. **2~3시간** | 단일 국가·단일 지표이고 BCRA에 공개 API가 있어 언제든 가능하다. BIS `WS_CBPOL`에 AR이 없는 것이 원인 |
| 7 | **중앙은행 결정문 JS 렌더링 5개국** (GB·MX·TR·SA·ZA) | 중간 | ① RSS/보도자료 API 우선 탐색(기관별 30분) → ② 없으면 헤드리스 렌더링 도입(워커 이미지에 Chromium 추가 · 이미지 크기·실행 시간·비용 증가). **탐색 반나절 + 렌더링 도입 시 1~2일** | 5개국 `cb_stance` 카드가 비어 있어 통화정책 비교의 공백이 크지만, 헤드리스 렌더링은 운영 비용·이미지 복잡도를 올리므로 RSS 탐색을 먼저 소진해야 한다 |
| 8 | **인도 여론조사** | 낮음~중간 | 영문 위키의 인도 문서가 득표율이 아닌 **연합별 예상 의석표**여서, 득표율 조사 문서(또는 인도 집계 사이트)를 먼저 탐색 → `llm/sources.yaml` 항목 교체 → 규칙 파서 검증. **반나절** | 정치 화면에서 인도만 비지만, 원천 자체를 찾는 탐색이 선행되어 결과가 불확실하다 |
| 9 | **튀르키예 여론조사** | 낮음~중간 | 영문 위키 문서가 없어 터키어 위키 또는 Politico 집계로 대체 → 문서 구조 확인 → 별칭(party_aliases) 추가 → 파서 검증. **반나절** | 8번과 같은 성격. 언어가 달라 정당명 별칭 매핑 작업이 추가된다 |
| 10 | **Ember 유로존 보충** | **완료** (2026-09-20 19:41, 3.11절) | 완료 — API `entity=EU` 집계 엔티티 요청 분리 + 미제공 국가 CSV 보충 + 인증 쿼리 우선. 운영 검증에서 유로존 M 2026-08 API 수집 확인 | 후속 없음. 인도네시아·사우디 월간은 Ember 원천 자체가 없어 연간만 제공(정상) |
| 11 | **weekly_brief 첫 성공 확인** | 모니터링 | 내일 06:00 KST 자동 재시도(실패 소스는 `next_due`가 다음 날로 앞당겨짐) 결과 확인 → 실패 반복 시 Bedrock 재시도·타임아웃 점검. **확인 10분** | 부트스트랩에서 Bedrock 연결 끊김으로 1회 실패했을 뿐 코드 결함 증거는 없다. 재시도 경로가 이미 구현돼 있어 관찰이 먼저다 |
| 12 | **TradingAgents 뉴스 분석가에 매크로 스냅샷 주입** (제안서 4.8) | **높음** | 스냅샷 조회 도구 추가 → 분석가 프롬프트에 지표 표 주입 → `vintage ≤ 분석일` 조건으로 룩어헤드 차단 → 테스트. **1~2일** | 이 프로젝트가 종목 분석과 만나는 지점이고(핵심 시너지), 개정 이력·vintage를 이미 보존해 두었으므로 데이터 측 준비는 끝났다 |
| 13 | **API 키를 SSM SecureString으로 이동** | 낮음~중간 | 파라미터 2개를 SSM SecureString으로 등록 → 태스크 정의에 `secrets`로 ARN 참조 → 워커 역할에 `ssm:GetParameters`·`kms:Decrypt` 추가 → 재배포. **1~2시간** | 현재도 `NoEcho`로 콘솔 이벤트에는 남지 않지만 태스크 정의 환경변수로는 보인다. 다만 두 키 모두 **무료·읽기 전용**이라 노출 피해가 제한적이다 |
| 14 | **Parquet/Athena 내보내기** | 낮음 | S3 `macro/export/`에 일별 Parquet 덤프 → Glue 카탈로그 → Athena 뷰. **1~2일** | 현 규모(테이블 항목 10만 미만)에서는 DynamoDB Query로 모든 화면이 해결된다. 대규모 분석 수요가 생기면 착수 |
| 15 | **모바일 레이아웃 검수** (≤900px) | 중간 | 900/430px 폭에서 5개 뷰 확인 → 히트맵 가로 스크롤·드로어 전체화면·필터 줄바꿈 보정 → 스크린샷. **1~2시간** | 대시보드는 이동 중 조회가 잦고, 히트맵(20행 × 9열)·드로어가 좁은 폭에서 깨질 가능성이 구조적으로 높다 |
| 16 | **관리자 탭 "검토 대기 목록" 화면** | 낮음~중간 | `GET /api/macro/docs`를 `review_status=pending`으로 모아 보여주는 목록 뷰 추가(신규 API 없이 가능) → 승인/반려는 기존 버튼 재사용. **2~3시간** | 승인·반려 버튼은 이미 문서 카드에 있어 기능 공백은 없다. 다만 "검토할 것이 몇 건인지"를 한 화면에서 보려면 목록이 필요하다 |
| 17 | **개정치(vintage) 비교 뷰** | 낮음 | 드로어의 `revisions`(최대 10건)를 시계열 2개(현재값 vs 최초 발표값)로 그리는 차트 추가. **반나절** | 데이터는 이미 보존돼 드로어에서 표로 볼 수 있다. 시각화는 편의 개선 수준 |
| 18 | **이벤트 알림** (금리 결정 · 선거 D-7) | 낮음 | `DOC#election`·`cb_stance` 일정에서 D-7 판정 → EventBridge + SNS 또는 화면 배너. **반나절~1일** | 알림 채널(메일·슬랙)이 이 프로젝트에 아직 없어 부수 구성이 필요하고, 현재는 화면 접속 시 확인으로 충분하다 |
| 19 | **PR #1 머지** | **사용자 결정** | PR 본문의 코드 리뷰 포인트 확인 → 머지 → (필요 시) 기본 브랜치 배포 재확인 | 코드·배포·부트스트랩은 끝났고 남은 것은 머지 판단이다. 리뷰 포인트는 PR 본문에 정리돼 있다 |
| 20 | **첫 정기 실행 모니터링** (2026-09-21 06:00 KST) | 모니터링 | `INGEST#<source>/LATEST`의 status·`next_due` 확인 → 실패 소스 재시도 결과 확인 → LLM 토큰 사용량 확인(예산 2,000,000 대비). **확인 15분** | 케이던스·재시도·락은 코드 리뷰에서 수정된 지 하루가 지나지 않았고, 정기 기동 1회를 실제로 관찰해야 검증이 끝난다 |

---

## 6. 운영 참고

배포·장애 대응의 전체 절차는 [webui/macro/README.md](webui/macro/README.md)(2·3·6장)에 있고, 여기서는
이력을 읽는 사람이 자주 걸리는 3가지만 적습니다.

- **재배포 시 `.env` 필요** — `deploy.sh`는 루트 `.env`에서 `FRED_API_KEY`·`EMBER_API_KEY`·
  `MACRO_LLM_DAILY_TOKEN_BUDGET`·`MACRO_SCHEDULE_ENABLED` 4개만 골라 읽습니다. 키를 주지 않고
  배포하면 스택에 저장된 기존 키가 **빈 값으로 덮어써져** 해당 소스만 스킵됩니다.
- **수집 락 409** — 수집 중 `POST /api/admin/macro/refresh`는 409(진행 중 안내)를 돌려줍니다.
  락은 `CONFIG / LOCK#collect`이며 TTL 3시간이 지나면 다음 기동이 넘겨받습니다.
- **`MacroTable`은 Retain** — 스택에서 테이블이 제거·교체되어도 데이터는 남습니다. 같은 이름으로
  다시 만들려면 기존 테이블을 먼저 정리해야 합니다(재생성 절차는 README 6장).

---

## 7. 문서 지도

| 알고 싶은 것 | 문서 |
|---|---|
| **전체 경과**(요청 → 변경 → 이유, 미수행 목록) | **이 문서** |
| 저장소 전체 작업 이력(30항) | [작업이력.md](작업이력.md) |
| 웹 UI 변경 이력(2026-09-20 항목) | [webui/변경이력.md](webui/변경이력.md) |
| 구조·측정값 종합(그림 11개) | [webui/macro/구조와-작업내역.md](webui/macro/구조와-작업내역.md) |
| **어떻게 구현했는가**(함수·알고리즘·엣지 케이스) | [webui/macro/구현-상세.md](webui/macro/구현-상세.md) |
| 키·스키마·API의 최종 기준 | [webui/macro/CONTRACT.md](webui/macro/CONTRACT.md) |
| 운영(수동 실행·키·예산·검토 큐·장애) | [webui/macro/README.md](webui/macro/README.md) |
| 설계 배경·화면 설계·리스크 | [webui/G20-매크로-대시보드-제안.md](webui/G20-매크로-대시보드-제안.md) |
| 인프라·비용(5장) | [webui/아키텍처.md](webui/아키텍처.md) |
| 화면 목업 | [webui/mockups/g20-macro-mockup.html](webui/mockups/g20-macro-mockup.html) |
| 프로젝트 전체 그림 | [프로젝트-한눈에-보기.md](프로젝트-한눈에-보기.md) |
