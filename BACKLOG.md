# 백로그 (Backlog)

이 포크에서 하기로 했으나 미룬 작업 목록입니다. 우선순위·근거를 함께 기록합니다.

## B1. 하락장 편향 교정 검증 (full 모드 종단)

> 🏷️ **우선순위 태그: 실사용 검토 시 최우선.** 이 도구의 하락장 약세 판정을
> 근거로 실제 자본을 움직일 계획이 생기면, 다른 무엇보다 먼저 이 항목을 완료할 것.
> 그전까지는 낮은 우선순위 (연구용 단계에선 상승장 종단 + 양방향 재판정 신호로 충분).

**배경**: 강세 편향 교정(편향검증-실험-결과.md)은 상승장 12표본 종단 + 27표본
재판정으로 검증됐으나, **하락장에서 약세 등급을 제대로 내는지(약세 재현율)**는 미확인.
이 검증이 중요한 이유: 상승장 데이터만으로는 "증거에 반응하는 진짜 교정"과 "시장과
무관하게 낙관만 죽인 감쇠기"를 구별할 수 없다 — 하락장이 유일한 판별(반증) 실험.

**막힌 이유**: full 모드 백테스트가 데이터 라이브러리(yfinance 등) 내부의 타임아웃
없는 SSL 소켓 읽기에서 무한 정지 — 이틀간 3회 재발. 전역 `socket.setdefaulttimeout`과
SIGALRM watchdog(`run_backtest(combo_timeout=)`)을 넣었으나, C 레벨에서 EINTR 재시도가
신호를 삼켜 완전히 잡지 못함.

**해결안 (반나절 규모)**: 백테스트 러너를 **조합별 별도 프로세스**로 격리
(`subprocess` 또는 `multiprocessing` + `join(timeout)` 후 `terminate()`). 프로세스
경계에서 강제 종료하면 라이브러리 내부 정지와 무관하게 다음 조합으로 진행 가능.

**완료 조건**: `--tickers MSFT,AAPL,NVDA --start 2025-02-03 --end 2025-04-30
--every 10 --mode full`이 정지 없이 완주하고, 하락장 약세 등급 비율이 상승장보다
유의하게 높은지(= 교정본이 시장 방향에 반응하는지) 확인.

## B2. 리스크 토론·PM 단계의 실효성 재검토

**전수 조사 완료 (2026-08-02, 27개 원본 실행 로그)** — 단계별 등급 추적 결과:

| 전이 | 등급 밴드 변경 | 해석 |
|---|---|---|
| 리서치 매니저 → **포트폴리오 매니저(최종)** | **0/27** | 리스크 토론 3인 + PM이 최종 등급 밴드를 **한 번도** RM과 다르게 내지 않음 |
| RM → 트레이더 | 24/27 | 대부분 5단계(Overweight)↔3단계(Buy) **척도 차이**라 실제 이견 아님 |

**핵심 발견**: 리스크 관리 후반부(공격/보수/중립 토론자 3인 + PM)는 **독립적인
분석 텍스트를 실제로 생성**(PM 결정문 4,157자 ≠ RM 3,175자, 전문 비교 상이)하지만,
그 결과 **최종 등급 밴드는 27/27 모두 RM 판정과 동일**했다. 즉 파이프라인에서
가장 비싼 후반 4개 에이전트 단계(× 토론 라운드)가 등급에는 0의 기여를 한다.
- 참고: NVDA 2025-11-24는 트레이더가 Hold로 이탈했으나 PM이 RM의 Underweight로
  복귀 — PM이 트레이더를 무시하고 RM에 정렬. 즉 "가장 최근 발언 추종"이 아니라
  "항상 RM 밴드로 수렴"하는 패턴.

**교정 후 재조사 완료 (2026-08-02, Phase 3의 교정 후 상태 로그 13개, 추가 LLM 호출 0)**:
**RM → PM 등급 밴드 변경 0/13.** 교정으로 RM 등급이 다양해졌음에도(Overweight 6 /
Hold 5 / Underweight 2 — 더 이상 전부 강세 아님) PM은 13/13 모두 RM 밴드에 정확히
수렴. 교정 전(0/27)과 합쳐 **0/40** — 두 프롬프트 체제, RM 등급 전 범위에서 재현됨.
→ "고무도장"은 교정과 무관한 구조적 특성으로 확정.

**남은 한계**: 하락장 표본 1개(MSFT 2025-02-03)뿐이라 하락 국면 재현은 B1과 함께
확대 필요. 등급 밴드가 안 바뀌어도 포지션 사이징·손절가·리스크 서술은 이 단계가
추가하므로 "무가치"와는 다름 (등급 기여만 0).

**권고 (조사 종료, 실행은 사용자 결정)**: 후반부 4개 에이전트 단계가 등급에 0 기여함이
두 체제에서 확정됐으므로, **역할 재정의 리팩터링**이 비용 대비 효과가 크다 —
등급 밴드는 RM에서 확정하고, 리스크 토론+PM은 (등급 재판정이 아니라) 포지션
사이징·손절·리스크 플래그 산출에 집중하도록 프롬프트·스키마를 명시적으로 분리.
분석 깊이(depth)를 높일 때 리스크 라운드가 비용을 배가시키므로 절감 효과가 큼.
반대로 "심판 견제"를 살리려면 (b) PM에 밴드 변경 권한과 그 사용 기준을 명시.

## B3. 강세 편향 후속 원인 실험

- `debate_first_speaker=bear` A/B — 2N+1 재반박 구조에서 선발언자(Bull)가 양 끝
  발언을 차지하는 것이 순서 편향(Phase 1에서 부분 지지)에 기여하는지 분리 검증.
- 표본 100개 이상으로 확대해 교정 전후 판별력 차이의 통계적 유의성 확보.
