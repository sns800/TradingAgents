# 🔬 강세 편향 원인 검증 및 교정 실험

> 백테스트(백테스트-실험-결과.md)에서 드러난 **강세 편향**(전체 파이프라인 등급의
> 96%가 강세)의 원인을 분리 검증하고, 교정을 적용해 효과를 확인한 실험 기록입니다.
> 금융공학(백테스트 방법론)과 프롬프트 설계 관점에서 4단계로 진행했습니다.

## 배경 — 무엇이 문제였나

첫 백테스트에서 전체 파이프라인은 27개 결정 중 **26개(96%)를 강세**(Buy/Overweight)로
냈다. 반면 동일 모델(Bedrock Claude Sonnet 4.5)을 쓴 단일 LLM 베이스라인은 8/11/8로
균형 잡힌 분포를 보였다. **같은 모델이 파이프라인을 거치면 강세로 쏠린다** — 즉 편향의
뿌리는 모델이 아니라 파이프라인의 구조·프롬프트에 있다는 가설에서 출발했다.

## 실험 설계 원칙 (비용 효율)

핵심은 **비싼 전체 재실행을 최소화**하는 것. 저장된 27개 실행의 토론 기록을 재활용해
**"판정 계층만" 분리 재실행**하면, 전체 파이프라인 재실행의 1/30 비용으로 원인을
규명할 수 있다. 이 재판정(rejudge) 방식이 Phase 1·2의 핵심 도구다.

---

## Phase 0 — 프롬프트 포렌식 (편향 유발 문구 수색)

심판·PM·토론자 프롬프트에서 편향을 유발할 문구 8곳을 특정했다. 대표:

| 유형 | 위치 | 문구 (요약) |
|---|---|---|
| **Hold 회피·결단 강요** | `research_manager.py` | "강한 근거가 있으면 분명한 입장을 정하라; Hold는 양측이 진짜 대등할 때만" |
| **Hold 회피 (스키마)** | `schemas.py` ResearchPlan | "...그 외에는 더 강한 쪽에 서라" — 매 호출 주입 |
| 결단 강요 | `portfolio_manager.py` | "Be decisive..." |
| 기회 언어 과잉 | `aggressive_debator.py` | "트레이더 결정을 옹호하는 설득력 있는 논거를 만들라" (결정이 강세면 강세 증폭 루프) |
| 등급 척도 비대칭 | `portfolio_manager.py` | Buy엔 "strong conviction", Sell엔 없음; Underweight은 "이익 실현" 프레임 |

## Phase 1 — 원인 분리 재판정 실험 (27표본 × 5조건 = 135회)

저장된 토론 기록으로 **심판 판정만** 조건을 바꿔 재실행했다:

| 조건 | 강세% | Hold | control 대비 | 해석 |
|---|---:|---:|---:|---|
| control (현행) | 44.4% | 13 | 기준 | — |
| order-swap (발언 순서 반전) | 55.6% | 6 | +11.1p | 순서 민감성 있음(11/27 변경), 방향은 불명확 |
| **no-anti-hold (결단 문구 제거)** | **11.1%** | **24** | **−33.3p** | **주범 확정** |
| neutral-label (Bull/Bear→A/B) | 55.6% | 11 | +11.1p | 라벨 가설 **기각** (양방향 교란) |
| **score-first (점수 먼저)** | **0.0%** | **27** | **−44.4p** | 등급 단어를 먼저 못 말하게 하면 편향 소멸 |

### 결정적 관찰
- **루브릭 점수상 양측 논거는 대등했다** (score-first에서 bull 합계 9.9 vs bear 9.0). 즉
  증거는 대등한데 "결단하라"는 지시가 Hold를 금지하고, 결단을 강요당한 모델이
  체계적으로 강세로 기울었다.
- 질적 사례(NVDA 2025-12-08): control과 no-anti-hold의 판정문이 **동일한 사실**(PER
  15.6 vs 재고 +32%)을 인용하는데, control은 "베어를 반박했다"며 Buy, no-anti-hold는
  "충분히 반박 못 했다"며 Hold — **증거가 아니라 지시가 결론을 갈랐다.**

### 가설 판정
1. ✅ **주범: Hold 회피·결단 강요 문구** (제거 시 −33p, 점수 우선 시 −44p)
2. ❌ 라벨 프레이밍 — 기각
3. 🔸 순서 민감성 — 부분 지지 (다음 실험 후보: `debate_first_speaker=bear` A/B)
4. 💡 **추가 발견**: 원본 실행에서 RM 등급 == 최종 등급 **27/27** — 리스크 토론·PM은
   판정을 한 번도 바꾸지 않는 "고무도장". 편향 교정은 RM 단계 하나로 충분.

## Phase 2 — 교정 적용 (증거 비례 판정으로)

Phase 1이 보여준 함정: 문구를 그냥 **제거**하면 반대 극단(89% Hold)으로 넘어간다.
목표는 "Hold 금지"도 "전부 Hold"도 아닌 **증거 비례 판정**. 적용 내용:

1. **기저율 균형 문구**로 교체 (RM·PM·스키마): *"대형주 임의 표본에서 양/음 알파는
   대략 반반이다 — 낙관이나 행동 욕구가 아니라 증거가 등급을 정하게 하라. Hold는
   정당한 결론이며, 방향성 등급은 루브릭이 한쪽의 명확한 우위를 보일 때만."*
2. **루브릭 점수 선출력**: ResearchPlan에 3항목 × 양측 점수 6필드를 recommendation보다
   **앞 순서**로 배치 + 점수-등급 정합 소프트 결합 (강제 변환은 과교정이라 배제)
3. **PM 등급 척도 대칭화** (Sell에도 conviction, Underweight의 이익 프레임 제거)
4. **공격 토론자 에코 루프 해소** (트레이더 결정 옹호 → 증거의 공격적 해석 옹호)
5. **감성 Neutral 밴드 정당화**

## Phase 3 — 교정 효과 검증

### ① 재판정 파일럿 (27표본, 교정된 실제 프롬프트)

| 조건 | 강세% | Hold% | 약세% |
|---|---:|---:|---:|
| control (교정 전) | 44.4% | 48.1% | 7.4% |
| no-anti-hold (단순 제거) | 11.1% | 88.9% | 0% |
| **corrected (교정)** | **25.9%** | **55.6%** | **18.5%** |

교정본은 한쪽 극단이 아니라 **양방향으로 균형 이동**했다(강세→중립뿐 아니라
Hold→약세 3건, Hold→강세 1건). 목표 범위(강세 20~60% / Hold 30~60% / 약세 ≥10%)
전부 충족.

### ② 종단 파이프라인 확인 (교정된 코드로 실제 full 모드 재실행, 상승장 12표본)

**재판정이 아니라 전체 파이프라인을 처음부터 돌린** 결과 — 같은 상승장 구간:

| | 원본 full (27표본) | 교정 후 full (12표본) |
|---|---:|---:|
| **강세** | **96%** | **50%** |
| Hold | 0% | 42% |
| 약세 | 4% | 8% |

**강세 쏠림이 96%→50%로 급감.** 재판정 실험의 결론이 종단 파이프라인에서도
재현됐다 — 교정이 실효성 있음이 확인됐다.

> ⚠️ 종단 표본이 12개인 이유: full 모드 백테스트가 데이터 라이브러리 내부의
> 타임아웃 없는 SSL 읽기에서 무한 정지하는 인프라 문제가 반복됐다(이틀간 3회).
> 전역 소켓 타임아웃과 SIGALRM watchdog을 넣었으나 C 레벨 재시도가 신호를 삼켜
> 완전히 잡지 못했다. 하락장 표본에서의 약세 재현율 확인은 **백로그**로 이관
> (BACKLOG.md — 조합별 프로세스 격리 방식으로 러너 재작성 필요).

---

## 결론

1. **강세 편향의 원인은 프롬프트의 "결단 강요" 지시로 규명됐다** — 모델·데이터가 아니라
   설계 결함. 저장된 토론 기록 재판정이라는 저비용 방법으로 인과를 분리했다.
2. **교정으로 강세 쏠림을 96%→50%(종단)로 줄였고**, 과교정(전부 Hold) 없이 증거 비례
   판정에 도달했다. 교정된 프롬프트는 CLI·웹 UI 모두에 반영됐다.
3. **부수 발견**: 리스크 토론·PM 단계가 심판 판정을 27/27 그대로 통과시키는 고무도장
   구조 — 향후 이 단계들의 실효성 재검토가 필요(백로그).

## 재현 방법

```bash
export TRADINGAGENTS_LLM_PROVIDER=bedrock \
       TRADINGAGENTS_DEEP_THINK_LLM=us.anthropic.claude-sonnet-4-5-20250929-v1:0 \
       AWS_DEFAULT_REGION=us-east-1
# Phase 1·2 재판정 (저비용, ~1시간)
python scripts/bias_probe.py --conditions control,no-anti-hold,corrected
# Phase 3 종단 (교정 코드 기준)
python scripts/backtest.py --tickers MSFT,AAPL,NVDA --start 2025-09-01 --end 2025-12-31 --every 10 --mode full
```

원본 데이터: `~/.tradingagents/logs/bias_probe/{run_main,run_phase2_corrected}/`,
`~/.tradingagents/logs/backtest_phase3/`.
