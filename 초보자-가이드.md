# TradingAgents 초보자 가이드

> 💡 그림 중심의 전체 개요가 먼저 보고 싶다면 [프로젝트-한눈에-보기.md](프로젝트-한눈에-보기.md)를 추천합니다.
>
> 이 문서는 원본 저장소에는 없는, 한글화 작업과 함께 새로 추가된 안내서입니다.
> 프로그래밍이나 주식 트레이딩에 익숙하지 않은 분도 이 프로젝트가 "무엇을, 어떻게" 하는지
> 이해할 수 있도록 작성했습니다.

---

## 1. 이 프로젝트는 무엇인가요?

**TradingAgents**는 여러 개의 AI(LLM, 대규모 언어 모델)가 **실제 증권사의 트레이딩 팀처럼 역할을 나눠 협업**하며,
특정 주식을 "살까(BUY) / 팔까(SELL) / 보유할까(HOLD)"를 결정하는 연구용 프레임워크입니다.

사람 한 명이 모든 것을 판단하는 대신, 아래처럼 **전문가 역할을 맡은 AI 에이전트들이 순서대로 일합니다.**

```
[1단계] 분석가 팀 (4명이 병렬로 분석)
   ├─ 시장 분석가(Market Analyst)         : 차트·기술적 지표(MACD, RSI 등) 분석
   ├─ 감성 분석가(Sentiment Analyst)      : 뉴스·Reddit·StockTwits 여론 분석
   ├─ 뉴스 분석가(News Analyst)           : 세계 뉴스·거시경제 지표 분석
   └─ 펀더멘털 분석가(Fundamentals Analyst): 기업 재무제표·실적 분석
                     ↓
[2단계] 리서처 토론 (찬반 토론)
   ├─ 강세론자(Bull Researcher)  : "이 주식은 오를 것이다" 근거 제시
   └─ 약세론자(Bear Researcher)  : "이 주식은 위험하다" 근거 제시
                     ↓
   리서치 매니저(Research Manager): 토론을 심판하고 투자 계획 수립
                     ↓
[3단계] 트레이더(Trader): 모든 보고서를 종합해 매매 제안서 작성
                     ↓
[4단계] 리스크 관리 토론 (3가지 성향이 토론)
   ├─ 공격적(Aggressive/Risky) : 수익 기회를 강조
   ├─ 보수적(Conservative/Safe): 손실 위험을 강조
   └─ 중립적(Neutral)          : 균형 잡힌 시각 제시
                     ↓
[5단계] 포트폴리오 매니저(Portfolio Manager): 최종 결정 (BUY / SELL / HOLD)
```

⚠️ **주의**: 이 프레임워크는 **연구 목적**으로 만들어졌습니다. 실제 투자 조언이 아니며,
실전 매매에 그대로 사용하는 것은 권장되지 않습니다.

---

## 2. 폴더 구조 한눈에 보기

```
TradingAgents/
├── main.py                  ← 코드로 직접 실행하는 최소 예제 (여기서 시작!)
├── cli/                     ← 대화형 명령줄 인터페이스(CLI)
│   ├── main.py              ← `tradingagents` 명령의 본체 (화면 표시, 진행 상황)
│   └── utils.py             ← 종목/날짜/모델 선택 질문 처리
├── tradingagents/           ← 핵심 패키지
│   ├── default_config.py    ← 기본 설정 (사용할 LLM, 토론 횟수, 데이터 출처 등)
│   ├── agents/              ← AI 에이전트들의 정의
│   │   ├── analysts/        ← 1단계: 분석가 4명
│   │   ├── researchers/     ← 2단계: 강세/약세 리서처
│   │   ├── managers/        ← 리서치 매니저, 포트폴리오 매니저
│   │   ├── trader/          ← 3단계: 트레이더
│   │   ├── risk_mgmt/       ← 4단계: 리스크 토론자 3명
│   │   └── utils/           ← 에이전트가 쓰는 도구(데이터 조회 툴, 기억(memory) 등)
│   ├── dataflows/           ← 외부 데이터 수집 (야후 파이낸스, Alpha Vantage,
│   │                           Reddit, FRED 거시지표, Polymarket 예측시장 등)
│   ├── graph/               ← 에이전트들을 연결하는 실행 흐름(LangGraph 기반)
│   │   └── trading_graph.py ← 전체 시스템을 조립하는 가장 중요한 파일
│   └── llm_clients/         ← 각 LLM 회사(OpenAI, Google, Anthropic 등) 연결 담당
└── tests/                   ← 자동 테스트 코드
```

### 꼭 알아야 할 핵심 파일 3개

| 파일 | 역할 |
|---|---|
| `tradingagents/graph/trading_graph.py` | 에이전트들을 조립하고 실행하는 **엔진**. `propagate("NVDA", "2024-05-10")`을 호출하면 전체 파이프라인이 돌아갑니다. |
| `tradingagents/default_config.py` | 어떤 LLM을 쓸지, 토론을 몇 번 할지, 데이터를 어디서 가져올지 정하는 **설정 파일** |
| `cli/main.py` | 터미널에서 대화형으로 실행하는 **사용자 인터페이스** |

---

## 3. 설치 방법

### 준비물
- Python 3.10 이상 (3.12 권장)
- LLM API 키 1개 이상 (OpenAI, Google, Anthropic 등 — 유료 API 사용 시 **비용이 발생**합니다)

### 설치 절차

```bash
# 1. 저장소 복제 (이미 클론했다면 생략)
git clone https://github.com/TauricResearch/TradingAgents.git
cd TradingAgents

# 2. 가상환경(virtual environment) 만들기 — 프로젝트별로 패키지를 격리하는 안전장치
conda create -n tradingagents python=3.12
conda activate tradingagents
# (conda가 없다면: python3 -m venv .venv && source .venv/bin/activate)

# 3. 패키지 설치
pip install .
```

### API 키 설정

사용할 LLM 제공사의 API 키를 환경변수로 등록합니다. 예:

```bash
export OPENAI_API_KEY=sk-...        # OpenAI(GPT) 사용 시
export ANTHROPIC_API_KEY=sk-ant-... # Anthropic(Claude) 사용 시
export GOOGLE_API_KEY=...           # Google(Gemini) 사용 시
```

또는 `.env.example`을 `.env`로 복사한 뒤 키를 채워 넣어도 됩니다:

```bash
cp .env.example .env
```

주가 데이터는 기본적으로 야후 파이낸스(yfinance, 무료·키 불필요)를 사용하므로
LLM 키만 있으면 바로 실행할 수 있습니다.

---

## 4. 실행 방법

### 방법 A: 대화형 CLI (초보자 추천)

```bash
tradingagents        # 또는: python -m cli.main
```

화면의 안내에 따라 종목 코드(예: `AAPL`), 분석 날짜, LLM 제공사, 분석 깊이를 고르면
에이전트들이 일하는 과정이 실시간으로 표시됩니다.

- 미국 주식: `AAPL`, `NVDA`, `SPY`
- 한국 주식: `005930.KS`(삼성전자), `035420.KS`(네이버) — 야후 파이낸스 표기법(코스피 `.KS`, 코스닥 `.KQ`)
- 기타 해외 시장 예: 도쿄 `7203.T`(도요타), 홍콩 `0700.HK`(텐센트)
- 암호화폐: `BTC-USD`, `ETH-USD`

### 방법 B: 파이썬 코드로 직접 실행

```python
from tradingagents.default_config import DEFAULT_CONFIG
from tradingagents.graph.trading_graph import TradingAgentsGraph

config = DEFAULT_CONFIG.copy()
ta = TradingAgentsGraph(debug=True, config=config)

# NVDA(엔비디아)를 2024-05-10 시점 데이터로 분석
_, decision = ta.propagate("NVDA", "2024-05-10")
print(decision)   # 최종 결정: BUY / SELL / HOLD
```

---

## 5. 첫 분석 따라하기 (실습)

처음부터 끝까지 한 번 직접 해보는 코스입니다. 약 10~15분 걸립니다.

### 5-1. API 키 발급받기

아래 중 **한 곳**만 가입하면 됩니다. 모두 신용카드 등록이 필요한 유료 API입니다.

| 제공사 | 발급 페이지 | 참고 |
|---|---|---|
| OpenAI (GPT) | https://platform.openai.com/api-keys | 가장 무난한 선택 |
| Anthropic (Claude) | https://console.anthropic.com/settings/keys | |
| Google (Gemini) | https://aistudio.google.com/apikey | 무료 사용량(free tier)이 있어 테스트용으로 좋음 |

> 💰 **비용 감각**: 분석 1회(종목 1개, 기본 설정)에 보통 수백 원~수천 원 수준의
> API 요금이 나옵니다. 토론 횟수를 늘리거나 비싼 모델을 고르면 늘어납니다.
> 처음에는 저렴한 모델(예: `gpt-5.4-mini`)과 토론 1회로 시작하세요.

### 5-2. 키 등록하기

```bash
cd TradingAgents
cp .env.example .env
```

`.env` 파일을 열어 발급받은 키를 붙여넣습니다:

```
OPENAI_API_KEY=sk-여기에-붙여넣기
```

### 5-3. 실행하고 질문에 답하기

```bash
tradingagents
```

화면에 나오는 질문 순서와 추천 답변:

| 질문 | 처음이라면 이렇게 |
|---|---|
| 종목 코드 (Ticker) | `AAPL` (애플) — 데이터가 풍부해 실패 확률이 낮음 |
| 분석 날짜 | 그대로 Enter (오늘 날짜) |
| 분석가 팀 선택 | 그대로 Enter (전원 선택) |
| 분석 깊이 (Research Depth) | **Shallow** (가장 빠르고 저렴) |
| LLM 제공사 | 키를 등록한 곳 선택 |
| 모델 선택 | 목록에서 mini/저가형 모델 |

이후 화면에 에이전트들이 실시간으로 일하는 모습이 표시됩니다.
Shallow 기준 보통 5~10분 정도 걸립니다.

### 5-4. 결과 확인하기

실행이 끝나면 최종 결정(BUY / SELL / HOLD)이 화면에 표시되고,
전체 보고서가 파일로 저장됩니다:

```
~/.tradingagents/logs/AAPL/2026-07-31/
├── reports/
│   ├── complete_report.md   ← 전체 보고서 통합본 (이것부터 보세요!)
│   ├── analysts/            ← 분석가 4명의 개별 보고서
│   ├── research/            ← 강세/약세 토론 및 리서치 매니저 판정
│   ├── trading/             ← 트레이더 제안서
│   └── risk/                ← 리스크 토론 및 최종 결정
└── message_tool.log         ← 실행 로그 (문제 발생 시 원인 추적용)
```

`complete_report.md`를 열면 분석 전 과정을 한 번에 읽을 수 있습니다.

---

## 6. 자주 나는 오류와 해결법

| 증상 | 원인과 해결 |
|---|---|
| `command not found: tradingagents` | 가상환경이 꺼져 있거나 설치가 안 된 상태. `conda activate tradingagents` (또는 `source .venv/bin/activate`) 후 `pip install .` 재실행 |
| `Error: Analysis date cannot be in the future` | 분석 날짜가 미래 날짜. 오늘 이전 날짜로 입력 (형식: `YYYY-MM-DD`) |
| `Error: Invalid date format` | 날짜 형식 오류. `2026-07-31`처럼 하이픈 포함 형식으로 입력 |
| API 키 관련 오류 (`VendorNotConfiguredError`, 401 등) | `.env`의 키가 비어 있거나 오타. 키 발급 페이지에서 다시 복사해 붙여넣고, `.env` 파일이 실행 위치에 있는지 확인 |
| 429 / rate limit 오류가 반복됨 | 요청 제한에 걸린 상태. `.env`에 `TRADINGAGENTS_LLM_MAX_RETRIES=6`을 설정하면 자동 재시도로 버팁니다 |
| `NoMarketDataError` / 데이터 없음 | 종목 코드 오타이거나 해당 날짜에 거래 데이터가 없는 경우(휴장일, 상장 전). 코드를 확인하고 평일 날짜로 다시 시도 |
| 실행이 너무 오래 걸림 | 정상일 수 있음(Deep 설정 시 30분 이상). 급하면 Shallow 깊이 + 빠른 모델로 재실행 |
| 보고서가 영어로 나옴 | 이 포크의 기본값은 한국어이므로, CLI의 언어 선택 단계에서 다른 언어를 골랐거나 `TRADINGAGENTS_OUTPUT_LANGUAGE`가 다른 값으로 설정된 경우입니다. 환경변수를 지우거나 `Korean`으로 설정 |

> 🔍 원인을 모르겠을 때는 결과 폴더의 `message_tool.log`를 열어 마지막 부분의
> 오류 메시지를 확인하세요.

---

## 7. 자주 나오는 용어 풀이

| 용어 | 설명 |
|---|---|
| **LLM (대규모 언어 모델)** | GPT, Claude, Gemini처럼 글을 읽고 쓰는 AI. 이 프로젝트의 모든 에이전트의 두뇌 역할 |
| **에이전트(agent)** | 특정 역할(분석가, 트레이더 등)과 도구를 부여받은 LLM 인스턴스 |
| **LangGraph** | 여러 에이전트의 실행 순서(그래프)를 정의·실행해 주는 라이브러리. `graph/` 폴더가 이것을 사용 |
| **기술적 지표(technical indicator)** | 주가·거래량으로 계산하는 보조 지표. RSI(과매수/과매도), MACD(추세 전환) 등 |
| **펀더멘털(fundamentals)** | 기업의 재무제표, 실적 등 "본질 가치"에 대한 데이터 |
| **강세/약세(Bull/Bear)** | 강세론자는 주가 상승을, 약세론자는 하락을 전망하는 쪽 |
| **OHLCV** | 시가(Open)·고가(High)·저가(Low)·종가(Close)·거래량(Volume). 주가 데이터의 기본 형태 |
| **룩어헤드 편향(look-ahead bias)** | 과거 시점을 분석하면서 그 시점엔 알 수 없던 미래 정보를 몰래 쓰는 오류. 이 프로젝트는 뉴스/데이터 날짜를 걸러 이를 방지 |
| **백테스트(backtest)** | 과거 데이터로 전략을 시뮬레이션해 보는 것 |
| **체크포인트(checkpoint)** | 실행 중간 상태를 저장해 두었다가, 중단돼도 이어서 재개할 수 있게 하는 기능 |
| **리플렉션(reflection)** | 매매 결과(수익/손실)를 보고 에이전트가 "무엇을 잘못 판단했는지" 기록해 다음 결정에 반영하는 학습 기능 |
| **딥/퀵 사고 모델(deep/quick think)** | 어려운 판단(토론 심판, 최종 결정)은 비싸고 똑똑한 모델, 단순 작업은 빠르고 싼 모델을 쓰는 이중 구조 |

---

## 8. 설정 바꿔보기 (`default_config.py`)

자주 바꾸게 되는 설정:

```python
config = DEFAULT_CONFIG.copy()
config["llm_provider"] = "anthropic"        # LLM 제공사 변경
config["deep_think_llm"] = "claude-sonnet-5"  # 깊은 사고용 모델
config["quick_think_llm"] = "claude-haiku-4-5"# 빠른 작업용 모델
config["max_debate_rounds"] = 2             # 강세/약세 토론 왕복 횟수 (늘리면 더 깊은 토론, 비용↑)
config["output_language"] = "English"       # 보고서 출력 언어 변경 (이 포크의 기본값은 Korean)
```

> 💡 **이 포크는 기본 출력 언어가 한국어(Korean)입니다.** 별도 설정 없이도 분석 보고서와
> 최종 결정문이 한국어로 나옵니다 (원본 저장소의 기본값은 English).
> 에이전트 내부 토론은 추론 품질을 위해 영어로 유지됩니다.

환경변수로도 같은 설정이 가능합니다 (코드 수정 불필요):

```bash
export TRADINGAGENTS_OUTPUT_LANGUAGE=Korean
export TRADINGAGENTS_MAX_DEBATE_ROUNDS=2
```

---

## 9. 한글화 작업에 대하여

이 사본(fork)은 다음 원칙으로 한글화되었습니다:

1. **번역한 것**:
   - 모든 주석(`#`)과 docstring, README, CHANGELOG — 초보자용 부연 설명 추가
   - **CLI 화면 문자열** — 질문, 메뉴, 안내, 에러 메시지, 진행 상황 표시 등 사용자에게 보이는 텍스트
   - 설정·인프라 파일(.env.example, Dockerfile 등)의 주석
2. **번역하지 않은 것 (의도적)**:
   - **LLM 프롬프트 문자열** — 번역하면 모델의 출력 형식과 품질이 달라질 수 있어 영어 원문 유지. 대신 바로 위에 한국어 요약 주석을 달았습니다.
   - **`@tool` 함수의 docstring** — LLM에게 "도구 사용 설명서"로 전달되는 텍스트라 원문 유지
   - **에이전트/팀 이름** ("Market Analyst" 등) — 내부 상태 추적 키로도 쓰이므로 유지
   - **dict 키, 상태 값, 변수·함수 이름** — 프로그램 동작과 테스트가 이 문자열에 의존하므로 유지
3. **원본과 다른 동작 (의도적 변경)**: 기본 출력 언어가 English → **Korean** (환경변수 `TRADINGAGENTS_OUTPUT_LANGUAGE`로 변경 가능)
4. **그 외 코드 로직은 변경하지 않았습니다.** 원본과 동일하게 동작합니다.
