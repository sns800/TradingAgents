# TradingAgents 웹 UI — 변경 이력

> 최초 구성부터 현재까지 웹 UI 관련 모든 변경 사항의 기록입니다.
> 아키텍처 상세는 [아키텍처.md](아키텍처.md) 참고.

---

## 2026-07-31 — 최초 구성 (커밋 `dbff80d`)

**목표**: TradingAgents 웹 UI를 AWS에 구축. 서울 리전 사용(미지원 서비스만 us-east-1),
Bedrock 모델은 us-east-1 연결, 시스템은 외부 비노출 + CloudFront URL로만 접근.

구축 내용:

- **프론트엔드** (`webui/frontend/`): 한국어 다크 테마 SPA — 분석 시작 폼, 실행 목록(10초 폴링, 상태·결정 뱃지, 경과 시간), 마크다운 보고서 뷰어(marked.js 로컬 번들). 해시 라우팅.
- **Lambda API** (`webui/backend/api_handler.py`): 실행 생성(ECS RunTask 트리거)/목록/상세/보고서 조회. boto3만 사용해 zip 한 장으로 배포. 동시 실행 3건 제한.
- **Fargate 워커** (`webui/worker/`): 분석 1건당 태스크 1개. DynamoDB에서 파라미터를 읽어 TradingAgents 그래프 실행 → 한국어 보고서를 S3 업로드 → 상태 기록. 30초 하트비트.
- **인프라** (`webui/infra/template.yaml`, 서울 리전 CloudFormation 단일 스택 `tradingagents-webui`): S3×2(사이트/데이터), DynamoDB, ECR, ECS 클러스터+태스크 정의, Lambda+Function URL, CloudFront(+OAC×2), CodeBuild, IAM 역할 일체.
- **접근 제어(당시)**: CloudFront 함수로 Basic 인증. S3/Lambda는 OAC로 CloudFront 전용, 워커는 인바운드 없음.
- **리전 구성**: 시스템 전체 서울. Bedrock만 us-east-1 (워커 컨테이너 `AWS_REGION=us-east-1`, 시스템 리소스는 `HOME_REGION`으로 분리 접근). 모델: Sonnet 4.5(deep)/Haiku 4.5(quick).
- 로컬에 Docker가 없어 **CodeBuild로 이미지 빌드** (소스 zip을 S3에 올려 빌드).

### 구축 중 해결한 문제

1. **CloudFront→Lambda 403 (가장 큰 삽질)**: OAC가 서명한 요청을 Lambda가 계속 거부.
   원인은 권한 부족 — OAC용 Lambda URL은 `lambda:InvokeFunctionUrl`뿐 아니라
   **`lambda:InvokeFunction` 권한도 함께** 필요 (공식 문서에만 있는 요구사항).
   진단 과정에서 403→index.html 오류 페이지 매핑이 API 오류를 가려 제거함.
2. **Basic 인증 ↔ OAC 충돌**: 뷰어의 `Authorization` 헤더가 오리진까지 전달되면
   OAC SigV4 서명과 충돌 → CloudFront 함수에서 검증 후 헤더 삭제로 해결.
3. **OAC POST 본문 서명**: CloudFront OAC는 POST 본문을 서명하지 않으므로
   클라이언트가 `x-amz-content-sha256`(본문 SHA-256) 헤더를 직접 전송하도록 프론트 수정.
4. **오리진 요청 정책**: 커스텀 최소 정책을 시도했으나 `x-amz-content-sha256`은
   정책 허용 목록에 넣을 수 없는 예약 헤더 → AWS 권장 관리형 정책
   `AllViewerExceptHostHeader`로 확정.

---

## 2026-07-31 — 종단 검증 및 안정화 (커밋 `8ff5461`)

첫 실전 분석(SPY, 얕게)이 8분 진행 후 실패:
`ReadTimeoutError ... bedrock-runtime.us-east-1...`

- **원인**: botocore 기본 읽기 타임아웃 60초. 한국어 보고서처럼 긴 LLM 생성이 60초를 초과.
- **수정**: `tradingagents/llm_clients/bedrock_client.py`에서 읽기 타임아웃을 300초로
  연장 (`TRADINGAGENTS_BEDROCK_READ_TIMEOUT`으로 조정 가능). 이 수정은 웹 UI뿐
  아니라 CLI의 Bedrock 사용에도 적용됨.
- **보고서 경로 매핑 수정**: 실제 저장 경로가 `1_analysts/market.md`처럼 번호
  접두사가 붙는 것을 확인하고 프론트 한국어 레이블 매핑을 실경로로 교정.
- **재검증 성공**: SPY 분석 약 9분 완료, 최종 결정 "Underweight", 한국어 보고서
  13개 생성·열람 확인.
- 그 외: Rich 콘솔 줄바꿈/ANSI 코드에 취약하던 테스트 1건 보강.

---

## 2026-08-01 — 인증을 Cognito User Pool로 교체 (커밋 `13c65d0`)

**요청**: 계정을 기존에 구성된 Cognito User Pool로 다시 연결.

- 기존 **`household-ledger-users` 풀**(`ap-northeast-2_beRiSnPMY`, 가계부·rtms 공유)에
  앱 클라이언트 **`tradingagents-web`**(`11ikj0bqsifr90tf4b37h4t14p`) 추가 —
  기존 앱들과 동일한 패턴(시크릿 없음, USER_PASSWORD_AUTH, 액세스 1시간/리프레시 30일).
- **프론트엔드**: 로그인 화면 추가(이메일/비밀번호, SDK 없이 cognito-idp REST 직접 호출),
  토큰 localStorage 저장, 만료 1분 전 선제 갱신 + 401 시 1회 갱신 후 재시도,
  로그아웃 버튼, 헤더에 로그인 이메일 표시. 설정은 `config.js`로 분리.
- **Lambda API**: 모든 요청에서 `x-access-token` 헤더의 액세스 토큰을 Cognito
  `GetUser`로 검증. client_id(이 앱 클라이언트 한정)·token_use 클레임 확인,
  5분 검증 캐시. 미인증 401.
- **인프라**: CloudFront Basic 인증 함수·토큰 파라미터 제거,
  `CognitoClientId` 파라미터 추가.
- 토큰은 OAC 서명과 충돌하는 `Authorization` 대신 **`x-access-token`** 헤더로 전달.
- 검증: 임시 사용자로 로그인→API 호출 확인 후 삭제(풀에는 가족 계정 2개만 유지).

---

## 2026-08-01 — 브라우저 캐시 문제 수정 (이 문서와 함께 커밋)

**증상**: 인증 교체 배포 후 새로고침해도 로그인 화면이 나오지 않음.

- **원인**: S3 정적 파일에 `Cache-Control` 헤더가 없어, CloudFront는 새 파일을
  서빙하는데 **브라우저가 자체 휴리스틱 캐시로 구버전을 계속 사용**.
- **수정**: 모든 정적 파일에 `Cache-Control: no-cache` 설정(기존 객체 일괄 적용 +
  deploy.sh의 sync에 옵션 추가). 브라우저가 매번 ETag로 재검증하므로 이후 배포는
  즉시 반영됨 (변경 없으면 304라 부담 없음).
- 기존에 열려 있던 탭은 **강력 새로고침(Cmd+Shift+R / Ctrl+F5) 1회** 필요.
  이후로는 일반 새로고침으로 항상 최신 버전.
- 문서 추가: `webui/아키텍처.md`(구조·데이터·사용법), `webui/변경이력.md`(이 문서).

---

## 2026-08-01 — 동시 한도 설정화, stock.happymstn.com 도메인 연동

- **동시 분석 한도**: 3건 고정 → CloudFormation 파라미터 `MaxActiveRuns`(기본 10, 1~50).
  배포 시 `MAX_ACTIVE_RUNS=20 webui/infra/deploy.sh --skip-image`로 조정.
- **커스텀 도메인**: us-east-1 ACM 인증서(DNS 검증) 발급 후 CloudFront 별칭 연결,
  Route53 `happymstn.com` 존에 A/AAAA 별칭 레코드를 스택으로 관리.
  이후 접속 주소는 **https://stock.happymstn.com** (기존 CloudFront URL도 유효).
- **프레임워크 개선 반영**: 설계 분석 단기(#1~#8)·중기(#1~#7) 로드맵 전체가
  구현되어 워커 이미지에 반영됨 — 자세한 내용은 저장소 커밋 이력과
  설계분석-보고서.md 참고. 웹 UI에서 시작하는 분석은 등급 파서 한국어 지원,
  룩어헤드 차단, 토론 재반박 구조, 분석가 병렬 실행(분석 단계 단축) 등이 적용됨.

---

## 2026-08-06 — 종료된 실행 삭제 기능

- **API**: `DELETE /api/runs/{id}` 신설 — 종료(completed/failed/cancelled)된
  실행의 S3 보고서(`runs/{id}/` 전체)를 먼저 지우고 DynamoDB 레코드를 삭제.
  진행 중 실행은 409(먼저 취소 안내). S3 삭제 실패 시 레코드를 남겨 재시도
  가능하게 함(레코드가 먼저 사라지면 남은 객체가 접근 불가 쓰레기가 됨)
- **IAM**: API 역할에 `dynamodb:DeleteItem`, `s3:DeleteObject`(runs/*),
  `s3:ListBucket`(prefix runs/*) 추가
- **프론트**: 실행 상세에 '실행 삭제' 패널(확인 후 삭제 → 목록 이동),
  목록 행에도 작은 '삭제' 버튼(행 클릭과 분리, 확인 프롬프트)
- 테스트 8건 신설(tests/test_delete_api.py) — 상태 게이트·S3 페이지네이션·
  부분 실패 시 레코드 보존·무인증 401

---

## 2026-09-20 — G20 매크로·정치 대시보드 추가 (구현 완료 · 미커밋 · 미배포)

**요청**: G20(19개국 + 유로존)의 ① 정당 지지율 ② 금리·중앙은행 기조·통화량·달러 환율
  ③ 경제구조(산업 부가가치·주요 기업·수출 품목·전력 구성·에너지 의존도) ④ GDP·GNI·예산·국방비
  ⑤ PPI·CPI·집값을 **매일 수집·누적**하고, **월/분기/년 시계열**과 **관측치 1건 단위의
  출처·수집 시각·개정 이력**까지 확인할 수 있는 화면을 기존 대시보드에 탭 하나로 추가.

착수 전 확정한 **결정 5가지** (근거·대안은 제안서 9장):

1. **정치 대상 17개국 전부** — 중국·사우디(경쟁 정당 없음)·유로존(정부 지지율 없음)은
   `not_applicable` 문서로 "해당 없음" 사유를 명시해 빈칸과 구분한다.
2. **저장소는 DynamoDB 단일 테이블** `tradingagents-webui-macro`(+ S3에 원본 응답 보존).
   Parquet/Athena 내보내기는 Phase 3으로 미룸 — 현재 규모(50만 항목 미만)에 과한 구성.
3. **기존 CloudFormation 스택에 리소스 추가**, 워커 이미지는 분석·카탈로그와 **공용**.
   새 스택·새 이미지·새 파이프라인 없음(`command`만 교체).
4. **AI 판정은 검토 전에도 노출** + `ai_generated` 배지 · 신뢰도 · 인용 · 원문 링크 ·
   `review_status`(pending/approved/rejected) 배지. 관리자가 사후 승인·반려.
5. **금액은 USD 통일**, 한국만 원화 병기(프론트 표시 규칙) — 국가 간 비교 가능성 우선.

**진행 방식**: 제안서·동작 목업 → 사용자 결정 5가지 → 데이터 계약(CONTRACT.md) 고정 →
멀티에이전트 구현(1차 7개: 레지스트리·집계 / 저장소 / 정량 소스 A·B / LLM 계층 / 프론트 / 인프라·문서,
2차 3개: 계약 정합 / Lambda API / 수집기·로컬 검증 서버, 3차: 통합 후속 6건 / 읽기 전용 코드 리뷰 /
리뷰 지적 18건 수정) → 전체 테스트·실데이터 dry-run·실제 프론트 스크린샷 검수.

**변경** (최종 파일 목록):

- **수집기**: `webui/macro/collect.py`(진입점·소스별 실패 격리), `registry.yaml`(지표 사전),
  `schema.py`, `aggregate.py`(M/Q/Y 집계·YoY·지수화), `store.py`(DynamoDB/S3 쓰기),
  `sources/`(bis, oecd, worldbank, imf, fred, ember, owid, wits, yahoo_fx, companies),
  `llm/`(cb_statements, polls_wiki, energy_policy, weekly_brief)
- **계약·문서**: `webui/macro/CONTRACT.md`(키·스키마·API 단일 기준),
  `webui/macro/README.md`(운영: 수동 실행·키 설정·LLM 예산·검토 큐·장애 대응·라이선스 표기)
- **인프라** `webui/infra/template.yaml`: `MacroTable`(pk/sk, 온디맨드, PITR, `DeletionPolicy: Retain`),
  `MacroScheduleRule`(EventBridge `cron(0 21 * * ? *)` = 매일 06:00 KST, `MacroScheduleEnabled`로 on/off,
  실행 역할은 `CatalogScheduleRole` 재사용 — 권한이 태스크 정의 단위라 명령만 달라도 유효),
  워커 IAM에 매크로 테이블·`macro/*` 권한, API IAM에 매크로 테이블 조회/검토 쓰기·`macro/docs/*` 읽기,
  파라미터 5개(`FredApiKey`·`EmberApiKey` NoEcho, `MacroLlmModel`, `MacroLlmDailyTokenBudget`,
  `MacroScheduleEnabled`), 워커/Lambda 환경변수, 출력 `MacroTableName`
- **배포** `webui/infra/deploy.sh`: 매크로 파라미터 오버라이드 4개 추가(+ `.env`에서 필요한
  변수만 골라 읽기), Lambda zip에 `macro_api.py` 포함(없으면 경고 후 기존 방식)
- **워커 이미지** `webui/worker/Dockerfile`: `pyyaml>=6` 명시(레지스트리 로드)
- **API** `webui/backend/macro_api.py`(신규, `/api/macro/*` 10개 경로) +
  `api_handler.py`에서 위임
- **프론트** `webui/frontend/macro.js`(개요·국가 프로필·지표 비교·통화가치·정치 5뷰 + 관측치
  상세 드로어), `charts.js`(인라인 SVG: 라인·소형다중·히트맵·발산막대·누적막대·스파크라인),
  `index.html`·`app.js`에 탭 `#/macro` 추가(기존 뷰 동작 변경 없음)
- **문서**: `webui/아키텍처.md` 5장 신설(구성도·테이블 키·스케줄·API·화면·보안·비용,
  기존 "구성 요소별 소스 위치"는 6장으로), `webui/README.md`(서비스 목록·비용 표 갱신 —
  DynamoDB 2개·EventBridge 2개, 고정비 약 $4~7/월), `프로젝트-한눈에-보기.md` 4장 한 줄
- **수집기 추가 파일**: `webui/macro/fakeddb.py`(인메모리 DynamoDB/S3 페이크 — 테스트·dry-run·로컬 서버 공용),
  `webui/macro/devserver.py`(Cognito 없이 실제 프론트+Lambda 핸들러를 로컬에서 띄우는 검증 서버,
  `/dev-login`), `webui/macro/llm/{bedrock_json,meta,common,textutil}.py`, `webui/macro/llm/sources.yaml`
  (20개국 중앙은행 결정문 URL·위키 여론조사 문서·정부 에너지 정책 URL — 전부 접속 실측),
  `scripts/macro_smoke.py`(소스별 라이브 스모크), `.env.example`에 `EMBER_API_KEY` 등 항목, `.gitignore`에 `.macro_raw/`
- **테스트**: `tests/test_macro_{registry,aggregate,store,sources_a,sources_b,llm,api,collect}.py` — 409건,
  픽스처 `tests/fixtures/macro/`(실제 API 응답 축소본). 전체 스위트 1,467 passed
  (기존 실패 1건 `test_bedrock_provider::test_construction_when_extra_installed`은 셸 `AWS_REGION` 환경 의존, 무관)

**검증**: 소스 모듈은 BIS·OECD·World Bank·IMF(SDMX 3.0)·Ember 공개 CSV·OWID·WITS·Yahoo를 실제 호출해
시리즈 키·차원 순서·국가 커버리지를 확정(각 모듈 상단 주석과 `registry.yaml`의 `coverage_note`).
6개국 dry-run(관측치 5.5만 건)을 로컬 검증 서버에 올려 실제 프론트 5개 화면이 실데이터로 렌더링됨을
헤드리스 Chrome으로 확인. 읽기 전용 코드 리뷰에서 확정된 결함(케이던스 종료시각 기준 → 일간 소스 격일
실행, 실패 소스 재시도 부재, 여론조사 월 집계 가드, API 기간 상한 누락, 재수집 동시 실행, 링크 스킴,
미사용 IAM 액션 등)을 전부 수정하고 회귀 테스트 추가.

**미완료·후속** (사용자 결정 필요 항목 포함):
- **배포하지 않음** — `webui/infra/deploy.sh`(워커 이미지 재빌드 포함) 실행은 사용자 승인 후. 첫 실행은
  `--since 2015-01-01` 부트스트랩 1회 권장(미국 통화가치 지수 등 12개월 전 기준값 필요).
- **커밋하지 않음** — 8월 카탈로그 작업의 기존 미커밋 변경(api_handler.py·app.js·index.html·style.css·
  catalog·tests)과 같은 워킹트리에 있음. 분리 커밋 여부는 사용자 판단.
- 데이터 공백(소스 미제공): AR 정책금리(BIS), AR·SA 집값(BIS), KR·JP·CN·IN PPI(OECD KEI 미포함, 원천 2023-02 정지 →
  한국은행 ECOS 등 국가통계 소스 후속), IMF 통화량 US·EU·CN·GB·IN·SA(US는 FRED 폴백), 정부부채 9개국(IMF FM 미구현),
  중앙은행 결정문 GB·MX·TR·SA·ZA(목록 JS 렌더링), IN 여론조사(의석 예측표만), TR 위키 여론조사 문서 없음.
- Parquet/Athena 내보내기, SSM SecureString으로 API 키 이동, TradingAgents 뉴스 분석가에 스냅샷 주입(제안서 4.8)은 Phase 3.

**배포 후 후속 (2026-09-20 17:04~19:41)**: `deploy.sh` 배포 → 부트스트랩 수집(25분, LATEST 623·SNAPSHOT 20) → PR #1 →
FRED·Ember API 키 `.env` 적용 재배포(fred 3,612건, ember API 2,178건) → Ember 유로존 집계 엔티티(`entity=EU`)·미제공 국가 CSV 보충·
쿼리 인증 우선 보완(커밋 fc44ad1, 이미지 재배포, 유로존 M 2026-08 API 수집 확인). 시간순 상세는 [HISTORY.md](../HISTORY.md).

**이유**: 기존 뉴스 분석가는 FRED(미국)만 수치 근거로 써서 한국·일본·중국 종목 분석의
거시 근거가 정성적 서술에 머물렀습니다. 공식 통계 API에서 숫자를 모아 **개정 이력과 vintage까지
보존**하면 (ⓐ) 사람이 보는 매크로 대시보드가 되고, (ⓑ) `vintage ≤ 분석일` 조건으로 과거 시점
스냅샷을 만들 수 있어 **백테스트 룩어헤드 차단**과 같은 기준으로 분석 컨텍스트에 주입할 수
있습니다. 인프라를 새로 만들지 않고 기존 스택에 테이블·규칙 하나만 더한 이유는 운영 대상을
늘리지 않으면서(배포 스크립트·인증·도메인·이미지 그대로) 비용도 월 $5 내외로 묶기 위함입니다.
숫자는 공식 통계만 쓰고 LLM은 "숫자로 안 나오는 것"(중앙은행 기조 판정·여론조사 표 추출·
에너지 정책 요약)에만 한정해, 데이터 신뢰성과 비용을 동시에 관리합니다.

📄 설계 배경·화면 설계: [G20-매크로-대시보드-제안.md](G20-매크로-대시보드-제안.md) ·
동작하는 화면 목업: [mockups/g20-macro-mockup.html](mockups/g20-macro-mockup.html)
(렌더링 결과는 `mockups/screenshots/`) · 데이터 계약: [macro/CONTRACT.md](macro/CONTRACT.md)

📄 전체 경과: [../HISTORY.md](../HISTORY.md) · 세부 구현: [macro/구현-상세.md](macro/구현-상세.md)

---

## 부록: 웹 UI 이전의 저장소 이력 (참고)

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 2026-07-31 | `a4f9f76` | 프로젝트 전체 한글화(주석·독스트링·README·CHANGELOG) + 초보자 가이드 신규 |
| 2026-07-31 | `11ac9e6` | 설정·인프라 파일(.env.example, Dockerfile 등) 주석 한글화 |
| 2026-07-31 | `84701a7` | 초보자 가이드에 실습 코스·오류 대처 추가 |
| 2026-07-31 | `ad0820f` | 기본 출력 언어 English → Korean (보고서가 기본 한국어) |
| 2026-07-31 | `74623c3` | CLI 화면 문자열 한글화 (약 140개) |
| 2026-07-31 | `1c6a157` | CI 워크플로 주석 한글화 |
